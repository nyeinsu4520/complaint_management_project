import { Link } from "react-router-dom";

export default function HelpDeskAssigned() {
  // 🔹 Fake data (design only)
  const complaints = [
    {
      id: 201,
      title: "Cannot access internal portal",
      severity: "HIGH",
      status: "IN_PROGRESS",
      updatedAt: "2025-01-15",
    },
    {
      id: 202,
      title: "Email delivery delay",
      severity: "MEDIUM",
      status: "OPEN",
      updatedAt: "2025-01-14",
    },
    {
      id: 203,
      title: "Request for software upgrade",
      severity: "LOW",
      status: "IN_PROGRESS",
      updatedAt: "2025-01-13",
    },
  ];

  return (
    <div className="space-y-6">

      {/* Page Header */}
      <div>
        <h2 className="text-2xl font-bold text-[#071952]">
          Assigned to Me
        </h2>
        <p className="text-gray-600">
          Complaints currently assigned to you
        </p>
      </div>

      {/* Complaints List */}
      <div className="bg-white rounded-lg shadow divide-y">
        {complaints.map((c) => (
          <div
            key={c.id}
            className="p-4 flex items-center justify-between hover:bg-gray-50 transition"
          >
            {/* Left info */}
            <div>
              <h3 className="font-semibold text-lg">{c.title}</h3>
              <p className="text-sm text-gray-500">
                Last updated: {c.updatedAt}
              </p>

              <div className="flex gap-3 mt-2">
                <span
                  className={`text-xs font-semibold px-3 py-1 rounded-full
                    ${
                      c.status === "OPEN"
                        ? "bg-blue-100 text-blue-600"
                        : "bg-purple-100 text-purple-600"
                    }
                  `}
                >
                  {c.status}
                </span>

                <span
                  className={`text-xs font-semibold px-3 py-1 rounded-full
                    ${
                      c.severity === "HIGH"
                        ? "bg-red-100 text-red-600"
                        : c.severity === "MEDIUM"
                        ? "bg-yellow-100 text-yellow-700"
                        : "bg-green-100 text-green-600"
                    }
                  `}
                >
                  {c.severity}
                </span>
              </div>
            </div>

            {/* Actions */}
            <div className="flex items-center gap-3">
              <Link
                to="#"
                className="text-sm bg-[#37B7C3] text-white px-4 py-2 rounded-lg hover:bg-[#2faab6]"
              >
                Reply
              </Link>

              <button
                className="text-sm bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700"
              >
                Resolve
              </button>

              <button
                className="text-sm bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600"
              >
                Escalate
              </button>
            </div>
          </div>
        ))}
      </div>

    </div>
  );
}
